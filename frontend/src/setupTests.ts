// Infrastructure (filled in). This file runs once before the test suite.
//
// `@testing-library/jest-dom` adds friendly DOM matchers to `expect`, so your
// tests can say `expect(el).toBeInTheDocument()` and
// `expect(input).toHaveValue("hi")` instead of poking at raw DOM properties.
import "@testing-library/jest-dom/vitest";
